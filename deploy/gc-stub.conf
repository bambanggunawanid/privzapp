# Default /gc/count behavior: accept the beacon and drop it (single
#-container runs have no analytics sidecar). docker-compose mounts
# deploy/gc-proxy.conf over this file to enable real counting.
return 204;
